import type { MCPProvider } from '../../../../tool-server/mcp/MCPProvider.js';

// Add app-owned MCP provider implementations from this directory.
export const mcpProviders = [] satisfies MCPProvider[];
