# Standalone UI app configuration.
# Required by `vite dev --mode app` and `vite build --mode app`.
# This is public Vite build-time config, not a secret.
VITE_APP_NAME={{PROJECT_NAME}}

# Optional Vertesia endpoint overrides. Defaults in src/ui/env.ts target dev-preview.
# Uncomment and set these in .env.local to point at a different environment.
# VITE_STUDIO_URL=https://api.vertesia.io
# VITE_ZENO_URL=https://api.vertesia.io
# VITE_STS_URL=https://sts.vertesia.io
