# @vertesia/cli-darwin-arm64

Native macOS arm64 binary package for `@vertesia/cli`.

This package is installed as an optional dependency by `@vertesia/cli` on supported hosts. It is not intended to be installed directly.
