{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "lib/**",
        "tsconfig.tsbuildinfo",
        ".tsbuildinfo"
      ]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": [],
      "cache": true,
      "env": ["CI", "RUNNER_OS", "RUNNER_ARCH", "ImageOS", "ImageVersion"]
    },
    "typecheck:test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "gen:schemas": {
      "dependsOn": ["^build"],
      "outputs": [],
      "cache": false
    }
  }
}
